import { type PostCard } from '../../../models/post-card-list';

export class PostStoreRepository {
    // TODO взаимодействие с Redux здесь (предположительно)

    public readModel (): PostCard[] {
        return [];
    }

    public saveModel (postCardList: PostCard[]): void {

    }
}
